function refreshVotes(innerHTML) {
  const newVotesDiv = document.getElementById('votes-map');
  newVotesDiv.innerHTML = innerHTML;
}


refreshVotes('<%= j render "shared/votes", fixpoint: @fixpoint %>');
